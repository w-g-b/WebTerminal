{
  "name": "web-terminal",
  "version": "1.0.0",
  "description": "Web-based terminal with shell command execution",
  "main": "server/app.js",
  "scripts": {
    "dev": "node server/app.js",
    "server": "node server/index.js",
    "unified": "node unified-server.js",
    "build": "cd client && npm run build",
    "install-all": "npm install && cd client && npm install"
  },
  "keywords": ["terminal", "web", "shell", "websocket"],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2",
    "socket.io": "^4.7.2",
    "jsonwebtoken": "^9.0.2",
    "bcryptjs": "^2.4.3",
    "node-pty": "^1.0.0",
    "dotenv": "^16.3.1",
    "cors": "^2.8.5",
    "concurrently": "^8.2.2"
  }
}